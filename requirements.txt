# ─────────────────────────────────────────────────────────────────────
# Dependencias del proyecto (runtime — auditado contra código real)
# ─────────────────────────────────────────────────────────────────────
# Auditoría: src/ imports only.  Dev tooling via pyproject.toml extras.
# ─────────────────────────────────────────────────────────────────────

# UI
nicegui>=3.0.0
plotly>=5.18.0

# Scraping (browser automation + HTML parsing)
scrapegraphai>=1.0.0
playwright>=1.40
beautifulsoup4>=4.12

# Vector store
chromadb>=0.5.0

# HTTP client (Ollama, MiniMax API)
aiohttp>=3.9.0

# Numerical / data
numpy>=1.26
pandas>=2.0.0
scikit-learn>=1.3.0

# Data models + config
pydantic>=2.0
sqlalchemy>=2.0
pyyaml>=6.0

# Auth (bcrypt hashing — pinned for passlib 1.7.x compat)
passlib[bcrypt]>=1.7.4,<2.0
bcrypt>=4.0.0,<4.1

# PDF ingestion
pdfplumber>=0.10
pypdf2>=3.0
