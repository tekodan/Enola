# ─────────────────────────────────────────────────────────────────────
# TFM - Dependencias de PRODUCCIÓN (Coolify / deploy)
# ─────────────────────────────────────────────────────────────────────
# Versión slim auditada contra el código real (12 paquetes, no 33).
# Todo lo que está acá se importa en runtime en src/.
# Lo que NO está: sentence-transformers, gradio, langchain*, torch,
# ollama (package), pydantic-settings, aiosqlite, httpx, python-dotenv,
# python-dateutil, tqdm, rich, pytest*, mypy, ruff, black, isort, types-*
#
# Para dev local con tooling (pytest, mypy, ruff, etc.), usar:
#   pip install -r requirements.txt   (archivo completo legacy)
# o bien:
#   pip install -e ".[dev]"            (extras en pyproject.toml)
# ─────────────────────────────────────────────────────────────────────

# Scraping (browser automation + HTML parsing)
scrapegraphai>=1.0.0
playwright>=1.40
beautifulsoup4>=4.12

# Vector store (acceso DIRECTO, sin LangChain)
chromadb>=0.5.0

# HTTP client (a Ollama en localhost:11434)
aiohttp>=3.9.0

# Embeddings / similarity (numpy es runtime, no dev)
numpy>=1.26

# Data + models
pydantic>=2.0
sqlalchemy>=2.0

# Config
pyyaml>=6.0

# PDF ingestion
pdfplumber>=0.10
pypdf2>=3.0

# UI
streamlit>=1.30
